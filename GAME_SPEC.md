# 3D Laser Puzzle Game — Game Specification

> Status: Core Prototype Complete / Normal Mode Vertical Slice Planning
> Purpose: Team development reference + AI coding/context reference
> Language: Japanese
> Last updated: 2026-08-18

---

## 0. このドキュメントの目的

このドキュメントは、本ゲームの企画・仕様・設計意図をチーム内で共有するための仕様書である。

また、Git リポジトリ内に配置し、AI コーディング支援ツールやチャットAIにリポジトリを読み込ませた際の基礎コンテキストとして利用することも想定する。

## 0.1 現在の開発段階

コア体験を確認するための最小プロトタイプは完了している。

現在は、レーザーをゴールへ到達させる通常モードのコアプロトタイプから、操作・ステージ進行・セーブを含むバーティカルスライスへ移行する段階である。

次の開発目標は、以下を満たす3ステージを最初から最後まで遊べる状態にすることである。

- レーザーがゴールへ到達するとクリアになる
- 位置、角度、反射、遮蔽物を組み合わせた3ステージを遊べる
- 成功と未到達の理由を画面上で理解できる
- ステージをクリアし、次のステージへ進める

距離減衰と中継 / 増幅装置は通常モードを含む全モードで使用する。レーザーがゴールへ到達した際に必要出力を要求する条件だけを、プレイヤーが選択する難しいモードの発展ルールとして扱う。

分岐・合流、Steam連携、大量のステージ制作も、通常モードのバーティカルスライスを評価した後に着手する。

### AIが参照する際のルール

- **「確定仕様」** と明記された項目は、原則として変更せず実装方針を考えること。
- **「検討中」** と明記された項目は、未確定仕様として扱うこと。
- 明記されていない仕様を勝手に確定しないこと。
- 実装上の都合で仕様変更が必要な場合は、変更案・理由・影響範囲を提示すること。
- 本ゲームのコアは「3D空間」「レーザー経路」「発射位置の選択」「一人称視点と真上視点の使い分け」である。
- 利便性のために俯瞰視点から3D情報を見せすぎて、一人称視点の意味を失わせないこと。
- 数値・操作感・UI詳細など未定義の項目は、仮実装値として提案してもよいが、それを確定仕様として扱わないこと。

---

# 1. ゲーム概要

## 1.1 ジャンル

3D空間を利用したレーザーパズルゲーム。

## 1.2 基本コンセプト

プレイヤーは3Dフィールド内にレーザー発射装置を設置し、発射方向や各種ギミックを利用してレーザーをゴールの的まで到達させる。

通常モードでもレーザー出力は距離に応じて減衰し、中継 / 増幅装置で回復できる。ただし、レーザーをゴールへ到達させれば、到達時の出力に関係なくクリアになる。

難しいモードでは、通常モードと同じ距離減衰と中継 / 増幅装置を使用し、ゴール条件に必要出力の判定を追加する。難しいモードで追加される機能はゴール条件だけである。

また、本ゲームでは以下の2つの視点を使い分ける。

- 一人称視点
- ステージを真上から見る俯瞰視点

2つの視点では得られる情報が異なり、プレイヤーは視点を切り替えながら空間を把握し、レーザー経路を考える。

---

# 2. コアとなるゲーム体験

## 2.1 プレイヤーに考えてほしいこと

本ゲームでは主に以下を考えさせる。

1. どの位置からレーザーを発射するか
2. どの方向にレーザーを発射するか
3. どのギミックを経由させるか
4. レーザーの出力をどのように維持・回復するか
5. 3D空間の高さや遮蔽物をどのように利用するか
6. 一人称視点と俯瞰視点から得られる情報をどう組み合わせるか

## 2.2 設計上避けたい状態

以下の状態にならないように設計する。

- 数cm単位の位置調整を何度も繰り返すだけのゲーム
- 0.1度単位の角度調整を要求するゲーム
- 総当たりで装置を動かせば解けてしまうゲーム
- 俯瞰視点だけでほぼすべての情報を取得できる状態
- 一人称視点が単なる移動・作業用になってしまう状態
- ギミックが増えすぎて、何を考えればよいのか分からなくなる状態

パズルの理想は、微調整ではなく、

> 「この位置から、この経路を通せばよい」

という発見によって解けることである。

---

# 3. 基本ゲームループ

**確定仕様**

1. ステージを観察する
2. 俯瞰視点で全体の位置関係を確認する
3. 一人称視点で高さ・遮蔽物・細部を確認する
4. 発射装置の設置位置を選ぶ
5. 発射角度を調整する
6. 必要に応じてステージ内のギミックを利用する
7. レーザーを発射する
8. 結果を確認する
9. 失敗した場合は位置・角度・経路を再検討する
10. レーザーをゴールに当てるとクリア

難しいモードでは、手順10の条件へ「ゴールの必要出力以上であること」を追加する。

基本的な思考サイクルは以下を想定する。

**観察 → 仮説 → 設置 → 実験 → 修正 → 成功**

---

# 4. 視点システム

## 4.1 一人称視点

**確定仕様**

通常の探索・確認に利用する。

一人称視点では3D空間そのものを確認できる。

主な用途：

- 高さを確認する
- 壁やオブジェクトの側面を見る
- 壁の裏側を見る
- 小さな穴や通路を発見する
- 屋根や天井の下を確認する
- ギミックの向きや形状を確認する
- 発射装置を操作する
- 俯瞰では分からない情報を取得する

## 4.2 俯瞰視点

**確定仕様**

俯瞰視点はステージを**完全に真上から見る固定方向の視点**とする。

自由な3Dカメラとして扱わない。

### 可能な操作

\
- 必要に応じて画面の平行移動

### 原則として行わない操作

- カメラを傾ける
- 横方向からステージを見る
- 自由回転して3D形状を確認する

### 俯瞰視点の役割

- ステージ全体の位置関係を理解する
- 発射可能エリアを確認する
- ゴール位置を確認する
- 大まかなレーザー経路を考える
- 距離感を把握する

### 俯瞰視点で意図的に分かりにくくする情報

- 高さ
- 壁の側面
- オブジェクトの裏側
- 屋根の下
- 地下
- 立体的な穴や通路
- 高低差を利用したレーザー経路

俯瞰視点は「便利な神視点」ではなく、**平面として位置関係を整理するための視点**とする。

## 4.3 ドローンという設定

**確定仕様（世界観上の設定）**

俯瞰視点は、設定上はドローン等によって上空からステージを確認しているものとして扱う。

ただし、ドローンを自由に飛ばして斜め方向から見るゲームシステムにはしない。

現段階では、ドローンは俯瞰視点を自然に説明するための設定である。


## 4.4 視点切り替えと補助カメラ

**確定仕様**

- 視点切り替えはキーボードの数字キーを使用する。
- 俯瞰視点中はプレイヤーを移動できない。
- プレイヤーが実際に移動できる範囲は、ステージ内の青い枠で示された操作可能エリア内に限定する。
- 真上からの俯瞰視点だけでは壁の向こう側などを確認できない場面があるため、ステージによっては一部の範囲だけを確認できる固定カメラを用意する。

### 補助カメラの設計意図

補助カメラは自由視点ではなく、ステージ側で用意された限定的な情報源として扱う。

目的は、

- 真上視点だけでは得られない局所情報を提供する
- 一人称での探索と組み合わせて空間を理解させる
- プレイヤーに必要な情報を与えつつ、神視点化を防ぐ

ことである。

補助カメラの台数、配置、画角、切り替え方法はステージ設計に応じて決定する。

---

# 5. レーザー発射装置

## 5.1 基本仕様

**確定仕様**

プレイヤーはステージ内の指定された発射可能エリアにレーザー発射装置を設置する。

発射装置には以下の2つの主要パラメータがある。

- 設置位置
- 発射角度

したがってプレイヤーは、

> 「どこから撃つか」

と

> 「どちらへ撃つか」

の両方を考える。


## 5.2 台数・再配置・発射開始

**確定仕様**

- 1ステージ内の発射装置の台数はステージによって異なる。
- 発射開始前であれば、発射装置は回収・再配置できる。
- プレイヤーが「発射開始」に相当する操作を行った後は、発射装置の位置変更をできなくする。
- 発射装置そのものの高さは変更できない。

これにより、発射前は試行錯誤できる一方、発射後は配置結果を確認するフェーズとして明確に区切る。

---

# 6. 発射可能エリア

## 6.1 基本コンセプト

**確定仕様**

発射装置を完全自由配置にはしない。

ステージ側で青色などの分かりやすい**発射可能エリア**を設定し、その中に用意された離散的な区画・ポイントへ設置できるようにする。

これにより、

- 発射位置を考える面白さを残す
- 細かすぎる位置調整を防止する
- パズルとして正解候補を設計しやすくする

ことを狙う。

## 6.2 正方形エリアの例

例：正方形の発射可能エリアを3×3に分割する。

```text
┌───┬───┬───┐
│ 1 │ 2 │ 3 │
├───┼───┼───┤
│ 4 │ 5 │ 6 │
├───┼───┼───┤
│ 7 │ 8 │ 9 │
└───┴───┴───┘
```

プレイヤーは各区画の所定位置に発射装置を設置できる。

実装上は各区画の中心など、ステージ制作者が定めた設置ポイントにスナップさせる方式を想定する。

## 6.3 エリア形状

**確定仕様**

発射可能エリアはステージごとに異なる形状を利用できる。

例：

### 正方形・長方形

```text
● ● ●
● ● ●
● ● ●
```

### 直線

```text
●──●──●──●──●
```

### L字

```text
●──●──●
      │
      ●
      │
      ●
```

### 離れた複数エリア

```text
● ●          ● ●
```

重要なのは、発射可能エリアの形そのものを**ステージ設計のパズル要素**として利用できることである。


## 6.4 区画数と表示

**確定仕様**

- 発射可能エリア内の設置区画数は、1つのエリアにつき最大9個程度を目安とする。
- 区画は升目のように視覚的に分かる表示を行う。
- 区画の基本サイズは未定。
- 一人称視点と俯瞰視点での具体的な見せ方は未定。

区画数を増やしすぎず、候補を比較して考えられる範囲に抑える。

## 6.5 設計意図

例えば直線上に5つの発射地点がある場合、

- 左側：反射板には当てやすいが、ゴールまで遠い
- 中央：増幅装置を経由できる
- 右側：ゴールには近いが壁に遮られる

といった違いを作る。

プレイヤーは単に最もゴールに近い場所を選ぶのではなく、ステージ全体の構造を考えて発射地点を選択する。

---

# 7. 発射角度

## 7.1 角度調整

**確定仕様**

レーザーの発射角度は基本的に自由とする。

8方向・16方向などへの完全な離散化は行わない。

## 7.2 スナップ補助

**確定仕様**

細かな角度調整が作業にならないよう、適切な方向に近づいた際に**軽いスナップ・吸着補助**を入れる。

目的は正解を自動的に与えることではなく、

- 数度以下の微調整を減らす
- 「狙った場所に当てる」操作を快適にする
- プレイヤーの意図を補助する

ことである。

### スナップ対象候補

**検討中**

- 反射可能な壁
- 中継装置
- ゴール
- 一部ギミック
- ステージ側で指定された重要ポイント

### 注意

スナップが強すぎると正しい経路が簡単に分かってしまうため、あくまで操作補助として使用する。


### 現在の確定事項

- 発射角度の調整可能範囲は180度以下とする。
- スナップ強度は未定。
- スナップ対象は未定。
- スナップ時には視覚・音によるフィードバックを用意する予定。

---

# 8. レーザーシステム

## 8.1 基本挙動

**確定仕様**

レーザーは発射装置から直線的に進む。

レーザーがギミックや壁に接触した場合、それぞれの性質に従って、

- 反射
- 通過
- 遮断
- 中継
- 増幅
- 分岐
- 合流

などが発生する。

## 8.2 距離減衰

**確定仕様**

通常モードと難しいモードの両方で、レーザーに出力値を持たせ、進んだ距離に応じて減衰させる。

通常モードでは、到達時の出力をゴールのクリア条件に含めない。難しいモードでのみ、ゴール必要出力と比較する。

概念例：

```text
発射時 100
↓
移動
↓
80
↓
移動
↓
50
```

具体的な減衰式・単位・初期値は未確定。


### 現在の確定事項

- 距離減衰は通常モードと難しいモードの両方で有効にする。
- ゴール必要出力によるクリア判定は難しいモードでのみ有効にする。
- レーザー初期出力はステージごとに設定する。
- 反射時に追加の出力減衰は発生しない。
- レーザーは瞬間発射ではなく常時照射とする。
- 発射後はレーザーが通った経路を継続して確認できるようにする。

### 未確定

- 距離減衰式
- 最大距離
- レーザーの進行速度

### 距離減衰の目的

- 最短ルート以外の要素を考えさせる
- 中継・増幅ギミックに意味を持たせる
- 発射位置の選択を重要にする
- 難しいモードでは「届くかどうか」だけではなく「十分な出力で届くか」を考えさせる

---

# 9. ゴール

## 9.1 クリア条件

**確定仕様**

通常モードでは、ゴールとなる的へレーザーを当てるとステージクリアとなる。

難しいモードでは、通常モードの条件に加えて、ゴールごとに設定された必要出力以上のレーザーを当てる必要がある。

難しいモードに限り、レーザーがゴールへ到達しても、到達時の出力が必要出力未満の場合はクリアにならない。

### ゴールの状態

プレイヤーが失敗理由を判断できるよう、少なくとも以下の状態を区別する。

- 通常モード：レーザー未到達
- 通常モード：レーザー到達・起動
- 難しいモード：レーザー到達・出力不足
- 難しいモード：必要出力を満たして起動

具体的な色、数値、ゲージ、エフェクトは未確定とする。



---

# 10. ギミック

## 10.1 反射する壁

**確定仕様**

レーザーを反射する。

入射方向に応じて反射方向が決まる。

反射計算の詳細は実装時に定義する。

## 10.2 反射しない壁

**確定仕様**

レーザーが当たると遮断される。

## 10.3 中継 / 増幅装置

**確定仕様（基本概念）**

通常モードと難しいモードの両方で、距離減衰したレーザーを中継し、出力を回復させる装置。

これにより、単純な直線距離では到達できないゴールへのルートを構築できる。

難しいモード専用の機能にはしない。

### 詳細ルール

**確定仕様**

- 1つの中継 / 増幅装置は、ステージ内で1回まで使用できる。
- 入射方向による制限は設けない。
- 出力回復方式は、受け取った出力へステージごとの設定値を加算する一定量回復とする。

具体的な回復量、最大出力を設けるか、装置を作動させる最低入力値は未確定とする。


---

# 11. 将来的な追加ギミック候補

以下はアイデア段階であり、初期実装の必須要件ではない。

## 11.1 高さを変更する反射装置

レーザーを上下方向へ反射し、別の高さへ送る。

3D空間であることを活用するための候補。

## 11.2 一方向透過ギミック

特定方向からのレーザーは通過し、逆方向では反射・遮断する。

## 11.3 分岐装置

1本のレーザーを複数に分割する。

難しいモードでは、出力も分割される仕様を候補とする。通常モードでは、出力配分をクリア条件に使用しない。

例：

```text
100
 ↓
分岐
↙   ↘
50   50
```

## 11.4 合流装置

複数のレーザーを1つにまとめる。難しいモードでは、出力を合算する仕様を候補とする。

例：

```text
40 ─┐
    ├→ 90
50 ─┘
```

---

# 12. ステージ設計方針

## 12.1 基本原則

**確定方針**

1ステージでプレイヤーに考えさせる中心テーマを増やしすぎない。

新しい要素は段階的に導入する。

## 12.2 想定 progression

### 序盤

- レーザー発射
- 発射角度
- 基本的な反射
- ゴール

### 次の段階

- 発射位置の選択
- 3×3などの発射可能エリア
- 壁による遮蔽

### 中盤

- 一人称と俯瞰視点の使い分け
- 高低差
- 複数の発射位置と反射経路の組み合わせ
- 距離減衰
- 中継 / 増幅装置

### 後半候補

- 複数の発射可能エリア
- 直線・L字・不規則な発射エリア
- 分岐
- 合流
- 一方向ギミック
- 複数ゴール
- 複数階層
- 一人称でしか発見できない経路

## 12.3 重要な考え方

難易度は、

- 設置候補を極端に増やす
- 角度調整を極端に細かくする

ことで上げない。

代わりに、

- 情報の組み合わせ
- 空間理解
- ギミックの組み合わせ
- 発射位置の意味
- 出力管理

によって難しくする。


## 12.4 ステージ構造・クリア評価

**確定仕様**

- ゲームはステージ制とする。
- 1ステージの想定プレイ時間は約5分を目安とする。ただし現時点では暫定値。
- ステージ数は未定だが、最大30ステージ程度を想定する。
- チュートリアルを用意する。
- ゴール条件を満たせばステージクリアとし、追加のスコア・ランク・星評価などは設けない。
- 開発側が想定したルート以外であっても、仕様上正しくゴールへレーザーを当てられた場合はクリアとして扱う。
- 別解は原則として許容する。

### 設計意図

プレイヤーに「開発者が用意した唯一の手順」を当てさせるのではなく、システムを理解したうえで成立した解答を認める。

ただし、当たり判定の抜けや想定外のバグによる突破は別解ではなく不具合として扱う。

## 12.5 難易度モード

**確定仕様**

- 通常モードでは、レーザーがゴールへ到達するとクリアになる。
- 距離減衰と中継 / 増幅装置は、通常モードと難しいモードの両方で有効にする。
- 難しいモードでは、ゴール到達時の出力をゴール必要出力と比較する。
- 難しいモードで通常モードから追加・変更される機能は、ゴールのクリア条件だけとする。
- 難しいモードでも、距離減衰式、中継 / 増幅装置の効果、ステージ内の基本挙動は通常モードと共通にする。

**未確定**

- モード選択をゲーム開始時、ステージ選択時、またはステージ開始時のどこで行うか
- 難しいモードのアンロック条件
- 通常モードと難しいモードで同じステージ構造を使うか
- モードごとにクリア状況を分けて保存するか
- ゲーム内で表示する正式なモード名

---

# 13. UI / フィードバック

## 13.1 必要になりそうなUI

**検討中**

- 現在のレーザー出力
- ゴールに必要なレーザー出力（難しいモード）
- 発射可能エリア
- 選択中の発射地点
- 一人称 / 俯瞰視点の切り替え
- リセット
- 装置操作
- レーザー発射

## 13.2 発射可能エリア

**確定方針**

発射可能エリアは青色系など、ステージ背景と明確に区別できる表現を使用する。

個々の設置可能区画・ポイントがプレイヤーから認識できるようにする。

具体的な色・マテリアル・エフェクトは未確定。

## 13.3 レーザー出力表現

**検討中**

通常モードと難しいモードの両方で距離減衰を理解しやすくするため、以下のいずれか、または組み合わせを検討する。

ゴール必要出力との比較表示は、難しいモードでのみ使用する。

- 数値
- 色
- 明るさ
- 太さ
- パーティクル量

---

# 14. 開発状況と今後の実装計画

## 14.1 完了したコアプロトタイプ

**完了**

以下の基本ループを遊べる状態まで実装した。

- 3Dステージ
- 一人称での歩行とマウス視点操作
- 真上固定の俯瞰視点
- 数字キーによる視点切り替え
- 俯瞰視点中のプレイヤー移動停止
- 発射可能エリア
- 複数の候補地点へのスナップ配置
- レーザーの常時照射と経路表示
- 通常壁による遮断
- 壁面での反射
- ゴールへの到達判定とクリア表示

このゴール判定は通常モードの正式な条件である。難しいモードを選択した場合だけ、必要出力の判定を追加する。

角度スナップ、ジャンプ、しゃがみ、操作可能範囲の制限など、確定仕様であっても上記のコアプロトタイプ完了項目に含まれない機能は、実装済みとして扱わない。

## 14.2 次の目標：通常モード3ステージのバーティカルスライス

距離減衰と中継 / 増幅装置を含め、位置、角度、反射、遮蔽物、2つの視点を使う3ステージを作る。

完成条件：

- レーザーがゴールへ到達するとクリアになる
- レーザー出力が距離に応じて減衰する
- 中継 / 増幅装置によって出力を回復できる
- 通常モードでは到達時の出力が低くても、ゴールへ到達すればクリアになる
- 3ステージで段階的に操作とルールを学べる
- 発射前の再配置と、発射後の結果確認を行える
- リトライ、クリア、次ステージへの遷移ができる
- 一度終了してもクリア済みステージが保持される
- ConsoleにErrorがなく、反射経路で無限処理が発生しない

## 14.3 実装順序

### フェーズ0：現在の完成状態を基準化する

目的は、今後の変更によって完成済み機能が壊れたことを検出できるようにすることである。

基準シーン、クリア手順、回帰確認項目、Console基準、Inspector調整値は `docs/PHASE0_BASELINE.md` に記録する。

2026-08-18時点で基準資料の作成、既存Consoleログの分類、仮パラメータのInspector整理は完了した。Playモードの手動回帰結果は基準資料のチェックリストへ実施ごとに記録する。

1. 現在のプロトタイプシーンとクリア手順を記録する
2. レーザー、反射、遮断、ゴール、移動、カメラ、配置の確認項目を作る
3. Consoleの既存ErrorとWarningを整理する
4. 仮の数値をInspector等から変更できるようにする

### フェーズ1：レーザー経路と描画を分離する

通常モードの距離減衰と、将来の分岐・合流の土台として、レーザー経路をデータとして扱えるようにする。

経路データは少なくとも以下を持つ。

```text
LaserPath
├── Segments
│   ├── Start
│   ├── End
│   ├── Length
│   └── HitObject
├── TotalLength
├── InitialPower
└── FinalPower
```

推奨する責務分割：

- 経路計算：Raycast、遮断、反射、経路点の生成
- 出力計算：経路長とギミックから各地点の出力を計算
- 描画：計算済みの経路をLineRenderer等で表示
- ゴール判定：通常モードでは到達を判定する
- 発射状態管理：発射前、発射中、リトライを管理

難しいモードでは、通常モードと同じ出力計算結果をゴール必要出力との比較にも利用する。

経路計算のクラス名やデータ型は、既存コードを確認したうえで決定する。

完了条件：

- LineRendererを使わなくても経路点と総距離を取得できる
- 減衰式を変更してもRaycast処理を変更する必要がない
- 既存の遮断、反射、ゴール到達判定が維持されている

### フェーズ2：距離減衰を実装する

通常モードと難しいモードの両方で使用する。

最初の比較用仮実装には、理解しやすい線形減衰を使用する。

```text
到達出力 = max(0, 初期出力 - 総移動距離 × 1メートルあたりの減衰量)
```

実装項目：

1. 初期出力と距離あたり減衰量を調整可能にする
2. 各反射区間の長さと総移動距離を計算する
3. ゴールへ到達出力を渡す
4. 総距離と現在出力を確認できるデバッグ表示を作る
5. 通常モードでは出力値に関係なく、到達すればクリアになることを確認する

完了条件：

- 通常モードと難しいモードの両方で、遠い経路ほど到達出力が小さくなる
- 通常モードでは必要出力の比較を行わない
- 出力が0未満にならず、NaNやInfinityが発生しない
- Inspector等の設定だけで検証値を変更できる

### フェーズ3：中継 / 増幅装置を実装する

通常モードと難しいモードの両方で、距離減衰した出力を回復する装置として追加する。

最初は、受け取った出力へ設定値を加算する1種類の装置だけを実装する。

実装項目：

1. レーザーとの相互作用を専用コンポーネントとして表す
2. 入力出力と使用済み状態を管理する
3. 1回までという使用制限を適用する
4. 作動中、未作動、使用済みの見た目を区別する
5. 経路ID、訪問回数、最大相互作用回数などで無限ループを防ぐ

完了条件：

- 通常モードと難しいモードの両方で出力を回復できる
- 同じ照射中に1つの装置が複数回出力を回復しない
- ループする配置でもフリーズしない

### フェーズ4：操作性と確定済み基本機能を補完する

以下を個別に実装・確認する。

1. 発射角度の自由調整
2. 軽い角度スナップ
3. スナップ時の視覚・音フィードバック
4. 発射前の回収・再配置と発射後の位置固定
5. プレイヤーのジャンプとしゃがみ
6. 青い操作可能範囲による移動制限
7. 俯瞰カメラのズーム

角度スナップは正解を示す機能ではなく、細かな微調整を減らす操作補助として評価する。

### フェーズ5：通常モードの3ステージを作る

#### ステージ1：発射とゴール

- 1つの発射候補地点からゴールへ直接当てる
- 発射角度の調整とゴール条件を教える
- 距離によって出力が変化することを表示する

#### ステージ2：反射

- 直接経路は壁に遮られる
- 反射壁を利用するとゴールへ到達できる

#### ステージ3：位置・反射・視点・増幅装置の組み合わせ

- 複数の発射候補地点がある
- 最もゴールに近い位置が必ずしも正解ではない
- 一人称と俯瞰視点から得られる情報を組み合わせる
- 正しい位置と反射経路を見つける
- 中継 / 増幅装置を通過すると出力が回復することを教える
- 通常モードでは、増幅装置を使わなくてもゴールへ到達すればクリアになる

この3ステージでゲーム性が確認できない場合は、難しいモードや追加ギミックへ進む前に、配置、操作、反射、視点、ステージ構造を見直す。

### フェーズ6：ステージ進行とローカルセーブを実装する

Steam連携より先に、ローカル環境でゲーム開始から再開まで成立させる。

最初に保存する候補：

- クリア済みステージID
- 到達済みの最高ステージ
- 音量、マウス感度などの設定

原則：

- シーン名ではなく安定したステージIDを保存する
- セーブデータにバージョン番号を持たせる
- 読み込めないデータは安全な初期値へ戻す
- 配置途中の状態保存は必要性を検証するまで実装しない

### フェーズ7：難しいモードのゴール必要出力を実装する

通常モードのバーティカルスライスを評価した後に着手する。

実装項目：

1. モード選択とルール切り替えを実装する
2. ゴールごとに必要出力を設定する
3. 難しいモードで到達出力と必要出力を比較する
4. 難しいモードで未到達、出力不足、起動を区別する
5. 通常モードでは出力不足によって失敗しないことを確認する

完了条件：

- 距離減衰と中継 / 増幅装置の挙動が両モードで同じである
- 通常モードではレーザーの到達だけでクリアできる
- 難しいモードでは必要出力未満でクリアにならない
- 難しいモードで追加される処理がゴール判定だけである

### フェーズ8：必要なステージだけに補助カメラを追加する

補助カメラは、基本の2視点だけでは確認できない情報が攻略に必要な場合に限り追加する。

追加前に、表示する情報、使用可能なタイミング、切り替え方法、画角をステージ単位で定義する。

### フェーズ9：分岐と合流を個別に試作する

経路システムと出力計算が安定してから着手する。

分岐では出力配分、最大分岐数、片方が遮断された場合の扱いを検証する。

合流では出力加算、最大出力、同時到達条件、合流損失の有無を検証する。

経路数と相互作用回数に上限を設定し、経路グラフが無制限に増えないようにする。

### フェーズ10：Steam連携と製品化準備

コアゲーム、ステージ進行、ローカルセーブが安定してから着手する。

候補はSteam実績、Steam Cloud、Steam Input、ストア用ビルドとする。対応範囲はバーティカルスライスの評価後に決定する。

## 14.4 優先順位

| 優先度 | 項目 | 依存先 |
|---|---|---|
| P0 | 現状の基準化と回帰確認 | なし |
| P0 | 経路計算と描画の分離 | 現在のレーザー実装 |
| P0 | 距離減衰と出力表示 | 経路データ |
| P1 | 中継 / 増幅装置 | 距離減衰 |
| P1 | 角度スナップなどの操作補完 | 基本の配置・回転 |
| P1 | 通常モードの3ステージ | 上記機能 |
| P1 | ステージ進行とローカルセーブ | 複数ステージ |
| P2 | 難しいモードの選択とルール切り替え | 通常モード完成 |
| P2 | 難しいモードのゴール必要出力 | 出力計算、モード切り替え |
| P3 | 補助カメラ | 必要なステージ設計 |
| P3 | 分岐・合流 | 安定した出力計算 |
| P4 | Steam連携 | 完成した進行・セーブ |

## 14.5 直近の作業チケット

以下を上から1件ずつ完了させる。

1. 現在のプロトタイプのクリア手順と回帰確認項目を記録する
2. レーザーの経路点、各区間長、総距離を取得できるようにする
3. 経路計算とLineRendererの更新を分離する
4. 仮の線形距離減衰と現在出力のデバッグ表示を実装する
5. 通常モードで出力値に関係なく、ゴール到達でクリアできることを確認する
6. 中継 / 増幅装置を実装する
7. 発射角度の自由調整と軽い角度スナップを完成させる
8. 発射前の再配置と発射後の位置固定を完成させる
9. ジャンプ、しゃがみ、操作可能範囲、俯瞰ズームを補完する
10. 通常モードの3ステージを作る
11. リトライ、ステージ遷移、ローカルセーブを実装する
12. 通常モードの3ステージを通してプレイテストする

難しいモードでは、この12件で完成した距離減衰と増幅装置を変更せず、ゴール必要出力の判定だけを追加する。

## 14.6 各機能共通の完了条件

- 正常系をPlayモードで確認した
- 代表的な異常系または境界値を確認した
- Consoleに新しいErrorがない
- 仮実装値と確定仕様が区別されている
- Inspector項目から調整できる値に、用途の分かる名前とTooltipがある
- 既存のレーザー、反射、遮断、ゴール、移動、カメラ、配置を再確認した
- 変更内容と確認方法を開発記録またはコミットメッセージへ残した

## 14.7 プレイテスト記録

```markdown
### テスト名

- 日付:
- 対象シーン:
- モード: 通常 / 難しい
- 検証した仮説:
- クリア経路の総距離:
- 初期出力:
- 距離減衰率:
- ゴール必要出力（難しいモードのみ）:
- 結果:
- 分かりにくかった点:
- 次に変更する点:
- 採用 / 保留 / 却下:
```

---

# 15. 実装設計に関する原則

この章は特定のゲームエンジンに依存しない方針である。

## 15.1 データ駆動を優先する

ステージごとの以下の情報は、可能な限りコードへのハードコーディングを避ける。

- 発射可能ポイント
- ギミック配置
- スナップ対象
- ステージ固有ルール
- レーザー初期出力
- 距離減衰パラメータ
- 難しいモードのゴール必要出力

## 15.2 ギミックを拡張可能にする

レーザーがオブジェクトに衝突した際、

「反射壁専用の巨大な条件分岐」

にならない構造を目指す。

新しいギミックを追加した際に、既存レーザーシステムの変更を最小限にできる設計が望ましい。

## 15.3 ゲームロジックと表示を分ける

可能な範囲で、

- レーザー経路・出力などのゲームロジック
- ビーム表示・パーティクルなどのビジュアル

を分離する。

これによりデバッグ・テストを容易にする。

## 15.4 未確定仕様をコードで固定しすぎない

特に以下は調整される可能性が高い。

- スナップ強度
- 発射地点数
- ギミックパラメータ
- 減衰率
- レーザー初期出力
- 難しいモードのゴール必要出力

調整可能なパラメータとして扱うことを推奨する。


## 15.5 技術スタック

**確定仕様**

- ゲームエンジン: Unity
- Unityバージョン: 6000.4.5f1
- レンダーパイプライン: Universal Render Pipeline 17.4.0
- 入力システム: Unity Input System 1.19.0
- 対応プラットフォーム: Windows
- 配信先: Steamでのリリースを想定
- 入力デバイス: PC向けキーボード・マウスを基本とする

**未確定**

- セーブデータの保存形式
- ステージデータ形式

---

# 16. 現在の確定仕様まとめ

2026-08-18 時点。

- 3D空間のレーザーパズルゲーム
- レーザーをゴールの的まで届ける
- 通常モードではレーザーがゴールへ到達するとクリアになる
- 通常モードと難しいモードの両方で、レーザー出力が距離に応じて減衰する
- 難しいモードのゴールには必要出力が存在する
- 反射する壁がある
- 反射しない壁がある
- 通常モードと難しいモードの両方に、出力を回復させる中継 / 増幅装置がある
- 一人称視点がある
- 真上固定の俯瞰視点がある
- 俯瞰視点ではカメラを傾けない
- 俯瞰視点ではズーム可能
- 俯瞰視点は世界観上ドローンで説明する
- 発射装置の設置可能範囲をステージ側で設定する
- 発射可能範囲は離散的な区画・ポイントに分割する
- 正方形3×3、直線など複数形状を利用できる
- 発射装置の位置選択自体をパズル要素にする
- 発射角度は自由
- 発射角度には軽いスナップ補助を入れる
- 微調整ではなく、位置・経路・空間理解によって解かせる
- プレイヤーはダッシュせず、一般的な歩行速度で移動する
- ジャンプ可能
- しゃがみ可能
- プレイヤーの移動可能範囲は青い枠内に限定する
- 視点切り替えはキーボードの数字キーを使用する
- 俯瞰視点中はプレイヤーを移動できない
- ステージによって限定範囲を映す補助カメラを設置できる
- 発射装置の台数はステージごとに異なる
- 発射前は発射装置を回収・再配置できる
- 発射開始後は発射装置の位置を変更できない
- 発射装置の高さ変更はできない
- 発射可能区画数は最大9個程度
- 発射可能区画は升目状に表示する
- 発射角度の調整範囲は180度以下
- 角度スナップ時に視覚・音のフィードバックを用意する予定
- レーザー初期出力はステージごとに異なる
- どちらのモードでも反射時の追加減衰はない
- レーザーは常時照射
- 発射後はレーザー経路を視認できる
- 中継 / 増幅装置はステージ内で1回まで使用
- 中継 / 増幅装置に入射方向制限はない
- ゲームはステージ制
- 1ステージ約5分を暫定目安とする
- 最大30ステージ程度を想定
- チュートリアルあり
- 追加スコア・評価システムは設けない
- 正しくゴール条件を満たす別解は許容する
- Unityで開発する
- Windows向け、Steamでのリリースを想定する
- PC向けキーボード・マウス操作を基本とする

---

# 17. 未確定事項

以下は今後の壁打ち・プロトタイプで決定する。

## 操作

- 発射装置の操作UI
- 一部範囲のみを映す補助カメラの切り替え方法
- 補助カメラの表示方式
- 一人称視点における青い操作可能範囲の見せ方

## 発射可能エリア

- 区画の基本サイズ
- 一人称視点・俯瞰視点それぞれでの具体的な見せ方

## 角度スナップ

- スナップ強度
- スナップ対象
- スナップ判定の具体的な角度幅
- 視覚・音フィードバックの具体的な表現

## レーザー

- 距離減衰式
- 最大距離
- レーザーの進行速度
- 常時照射中の更新方式・表示方式

## 中継 / 増幅装置

- 具体的な回復量
- 最大出力を設けるか
- 装置を作動させる最低入力値
- 作動中、未作動、使用済みの具体的な表現
- 出力不足がゴール条件にならない通常モードで、増幅装置を攻略上必須にする別の仕組みを設けるか

## ステージ

- 1ステージの正式な想定プレイ時間
- 正式なステージ数
- チュートリアルの構成
- 各ステージのアンロック方式
- クリア済みステージへの再挑戦方法
- 難しいモードの選択タイミング
- 難しいモードのアンロック条件
- 通常モードと難しいモードでステージ構造を共有するか
- モードごとにクリア状況を分けて保存するか
- 難しいモードの正式名称

## 技術

- セーブデータの保存形式
- ステージデータ形式
- Steam連携の範囲


# 18. AIに実装相談する際の推奨プロンプト

AIにこのリポジトリを読ませて相談する場合は、例えば以下のように依頼する。

```text
GAME_SPEC.md を最初に読んでください。

確定仕様と未確定事項を区別したうえで、
現在のゲーム仕様を変更せずに実装案を提案してください。

未確定事項について仮定が必要な場合は、
「仮定」と明示してください。

実装上、仕様変更を推奨する場合は、
変更内容・理由・プレイヤー体験への影響を説明してください。
```

機能実装時：

```text
GAME_SPEC.md の仕様を前提に、
[実装したい機能] の設計案を考えてください。

既存コードを確認し、
責務分割、必要なクラス/コンポーネント、
データ構造、処理フローを提案してください。

仕様書に書かれていない部分を勝手に確定しないでください。
```

レビュー時：

```text
GAME_SPEC.md と現在の実装を比較して、
仕様と異なる部分、
将来的に拡張しづらい部分、
ゲームプレイ上問題になりそうな部分を指摘してください。

重要度を High / Medium / Low で分類してください。
```

---

# 19. ドキュメント運用ルール

ゲーム仕様を変更した場合、このファイルも更新する。

推奨：

- 大きな仕様変更は Pull Request でレビューする
- コードだけ変更して仕様書を放置しない
- 未確定仕様が決定したら「確定仕様」に移す
- 廃止した仕様は削除または変更履歴に残す
- 実装都合とゲームデザイン上の仕様を混同しない

将来的に仕様が大きくなった場合は、以下のように分割してもよい。

```text
docs/
├── GAME_SPEC.md
├── LASER_SYSTEM.md
├── CAMERA_SYSTEM.md
├── LEVEL_DESIGN.md
├── GIMMICKS.md
└── TECHNICAL_DESIGN.md
```

現段階では仕様規模が小さいため、`GAME_SPEC.md` 1ファイルで管理する。

---

# 20. 変更履歴

## 2026-08-18

- フェーズ0の基準シーン、クリア手順、回帰確認項目、Console基準、Inspector調整値を記録
- 中央配置から反射してゴールへ到達する基準経路に合わせ、プロトタイプの反射壁角度を補正
- Inspector上の仮パラメータへ用途と調整方針のTooltipを追加
- 通常モードのクリア条件を「レーザーがゴールへ到達すること」に変更
- 距離減衰と中継 / 増幅装置は、通常モードと難しいモードの両方で使用する仕様として整理
- 難しいモードで追加される機能を、ゴール必要出力によるクリア判定だけに限定
- 次の開発目標を、距離減衰と中継 / 増幅装置を含む通常モード3ステージのバーティカルスライスへ変更
- 距離減衰と中継 / 増幅装置を通常モードの優先実装項目へ変更
- 難しいモードの選択方法、アンロック条件、進行保存方法を未確定事項へ追加

## 2026-08-17

- 開発状態を「コアプロトタイプ完了 / バーティカルスライス計画中」へ更新
- コアプロトタイプの完了項目を記録
- ゴール条件を「必要出力以上」に統一
- ゴールの未到達、出力不足、起動状態を定義
- 中継 / 増幅装置の一定量回復仕様と未確定パラメータを整理
- レーザー経路、出力計算、描画を分離する実装方針を追加
- 距離減衰からSteam連携までの実装順序と優先度を追加
- 3ステージのバーティカルスライス完成条件を追加
- 直近の作業チケット、共通完了条件、プレイテスト記録形式を追加
- Unity、URP、Input Systemの現在のバージョンを記録
- 発射可能エリア内の節番号を修正

## 2026-08-12

初版。

- 基本ゲームコンセプトを整理
- 真上固定の俯瞰視点を確定
- ドローンは俯瞰視点の世界観上の説明として採用
- 発射可能エリアを離散的な設置ポイントに分割する方式を採用
- 正方形3×3・直線などの発射エリア形状を採用
- 発射角度は自由とし、軽いスナップ補助を採用
- Git / AI参照を前提とした仕様書構成を作成
- プレイヤー操作範囲・ジャンプ・しゃがみ・視点切り替え仕様を追加
- 俯瞰視点中の移動禁止と限定補助カメラ方針を追加
- 発射装置の再配置・発射開始後の固定・高さ固定を追加
- 発射可能区画を最大9個程度とし、升目表示方針を追加
- 発射角度を180度以下とし、スナップ時フィードバック方針を追加
- レーザー常時照射・反射時追加減衰なし・経路表示を追加
- 中継 / 増幅装置の使用回数と入射方向制限を確定
- ステージ制・チュートリアル・スコアなし・別解許容を追加
- Unity / Windows / Steam想定の技術方針を追加
